#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# -------------------------------------------------------------------

# Copyright (c) 2025 Huawei Technologies Co., Ltd.
# This file is a part of the CANN Open Software.
# Licensed under CANN Open Software License Agreement Version 1.0 (the "License").
# Please refer to the License for details. You may not use this file except in compliance with the License.
# THIS SOFTWARE IS PROVIDED ON AN "AS IS" BASIS, WITHOUT WARRANTIES OF ANY KIND, EITHER EXPRESS OR IMPLIED,
# INCLUDING BUT NOT LIMITED TO NON-INFRINGEMENT, MERCHANTABILITY, OR FITNESS FOR A PARTICULAR PURPOSE.
# See LICENSE in the root of the software repository for the full text of the License.
# ===================================================================================================================
""" This file is GENERATED by bin/gen_esb, do not edit it manually"""
import ctypes
import os
from typing import List, Optional, Union
try:
    from ge.es import GraphBuilder, TensorHolder
    from ge.es.tensor_like import TensorLike, convert_to_tensor_holder, resolve_builder
    from ge.graph.types import DataType, Format
    from ge.graph.graph import Graph
    from ge.graph.tensor import Tensor
    from ge._capi.pyes_graph_builder_wrapper import (
        get_generated_lib,
        EsCTensorHolderPtr,
        EsCGraphBuilderPtr,
        EsCTensorPtr,
        EsCGraph
    )
except ImportError as e:
    raise ImportError(f"Unable to import ge module: {e}")

# 获取生成产物C实现的so句柄
try:
    esb_generated_lib = get_generated_lib("libes_hccl.so")
except RuntimeError:
    esb_generated_lib = get_generated_lib("libhccl.so")

# 定义 HcomReduceScatterV 函数原型
esb_generated_lib.EsHcomReduceScatterV.argtypes = [EsCTensorHolderPtr, EsCTensorHolderPtr, EsCTensorHolderPtr, EsCTensorHolderPtr, ctypes.c_char_p, ctypes.c_char_p]
esb_generated_lib.EsHcomReduceScatterV.restype = EsCTensorHolderPtr  # EsCTensorHolder指针

# 创建Python包装函数
def HcomReduceScatterV(x: Union[TensorHolder, TensorLike], recv_count: Union[TensorHolder, TensorLike], send_counts: Union[TensorHolder, TensorLike], send_displacements: Optional[Union[TensorHolder, TensorLike]] = None, *, reduction: str, group: str) -> TensorHolder:
    """
    Python wrapper for EsHcomReduceScatterV function
    Args:
        x: TensorHolder or TensorLike object
        recv_count: TensorHolder or TensorLike object
        send_counts: TensorHolder or TensorLike object
        send_displacements: TensorHolder or TensorLike object (optional)
        reduction: string
        group: string
    Returns:
        New TensorHolder object
    Note: at least one of the following input arguments should be TensorHolder object:
          x
          recv_count
          send_counts
          send_displacements
    """
    # 获取owner_graph_builder
    owner_graph_builder = resolve_builder(x, recv_count, send_counts, send_displacements)

    # 将TensorLike输入转换为TensorHolder
    x = convert_to_tensor_holder(x, owner_graph_builder)
    recv_count = convert_to_tensor_holder(recv_count, owner_graph_builder)
    send_counts = convert_to_tensor_holder(send_counts, owner_graph_builder)
    send_displacements = convert_to_tensor_holder(send_displacements, owner_graph_builder)

    # 调用C函数
    _result = esb_generated_lib.EsHcomReduceScatterV(x._handle, recv_count._handle, send_counts._handle, (send_displacements._handle if send_displacements is not None else None), reduction.encode('utf-8'), group.encode('utf-8'))

    # 创建新的Python包装对象
    _tensor_holder = TensorHolder._create_from(_result, owner_graph_builder)
    # 尝试获取scope内信息设置到节点上
    owner_graph_builder._apply_scope_infos_to_node(_tensor_holder)
    return _tensor_holder
